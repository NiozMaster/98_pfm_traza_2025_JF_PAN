{
  "name": "supply-chain-tracker-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate-config": "node ./scripts/generate-config.js",
    "update-abi": "node ./scripts/update-abi.js",
    "dev": "npm run update-abi && npm run generate-config && next dev",
    "build": "npm run update-abi && npm run generate-config && next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "ethers": "^6.8.0",
    "next": "^13.5.11",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@types/react": "19.2.7",
    "typescript": "5.9.3"
  }
}
